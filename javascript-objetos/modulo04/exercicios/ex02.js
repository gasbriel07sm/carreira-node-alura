const dados = require('./dados.json');

// console.log(dados);

const infoProdutos = dados.produtos;
const infoProdutosString = JSON.stringify(infoProdutos);
console.log(infoProdutos)
console.log(infoProdutosString)